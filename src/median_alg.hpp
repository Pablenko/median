#ifndef MEDIAN_ALG_HPP
#define MEDIAN_ALG_HPP

#include <string>
#include <utility>
#include <vector>

std::vector<float> median(const std::vector<int>& values, const std::vector<int>& breaks);

#endif
